import { chevronDown, iconToSvg } from '@verbb/plugin-kit-icons';

/** Down chevron for toolbar dropdown triggers — Plugin Kit glyph, not a CSS border hack. */
export const MENU_CHEVRON_SVG = iconToSvg(chevronDown);
