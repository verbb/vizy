/**
 * Toolbar Add Block, gutter `+`, and slash insert **Vizy Blocks only**.
 * Prose nodes (heading, list, …) stay on the formatting toolbar / typing.
 */
export const BLOCK_INSERT_KINDS = ['block'] as const;
